<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.medapp_hm131">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_medapp_red"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_medapp_red_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity_healthy_parametre"></activity>
        <activity android:name=".PressParametre" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>